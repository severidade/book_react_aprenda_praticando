const titulo = `livro 'React' do "Maujor"`;
const preco = 80;
const mensagem = 
  `O preço normal do ${titulo} e de R${preco},00
  na promoção o preço do livro cai para R${preco * 0.8},00`;

console.log(mensagem);


